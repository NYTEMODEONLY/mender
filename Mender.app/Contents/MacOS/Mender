#!/usr/bin/env bash
set -euo pipefail

APP_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
osascript <<APPLESCRIPT
tell application "Terminal"
  activate
  do script "cd " & quoted form of "$APP_DIR" & " && ./mender"
end tell
APPLESCRIPT
